"use client";
import Button from "../Button";


const NavbarFunctions = () => {
  return <section>
    <Button label="Sign Up" onClick={() => {}} whiteBg />
  </section>;
};

export default NavbarFunctions;
